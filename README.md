# Jobs

Твой персональный ИИ-ассистент в Telegram. Работает 24/7.

## Что это

Claude Code в Telegram — пишешь задачу, он делает. Полный доступ к терминалу, файлам, интернету.
Два транспорта: Telethon (userbot) и Bot API (aiogram) — параллельно или по отдельности.

```
— Напомни через час
— ✅ Напомню в 15:30

— Спарси hh.ru, найди python вакансии
— [пишет парсер, запускает, присылает результат]

— Поручи Маше отчёт к пятнице
— ✅ Задача создана, Маша уведомлена

— (в группе) @bot о чем говорили?
— [читает лог группы, отвечает]
```

**Не чат-бот. Агент с полным доступом к системе.**

---

## Быстрый старт

```bash
git clone https://github.com/qanelph/jobs.git && cd jobs
cp .env.example .env
# Заполни хотя бы один транспорт + TG_OWNER_IDS
docker-compose up
```

При первом запуске — ввод телефона (Telethon) и/или OAuth.

---

## Что умеет

| Функция | Описание |
|---------|----------|
| **Задачи** | Код, скрипты, парсеры, автоматизация |
| **Голос** | Whisper транскрипция → выполнение |
| **Файлы** | Фото/документы → обработка |
| **Планировщик** | Напоминания, отложенные, повторяющиеся |
| **Память** | Помнит контекст между сессиями |
| **Heartbeat** | Сам пишет когда нужно |
| **Подписки** | Следит за каналами, реагирует на посты |
| **MCP** | 700+ интеграций через чат |
| **Плагины** | Расширения из маркетплейса |
| **Мульти-юзер** | Управление задачами других людей |
| **Мульти-owner** | Несколько владельцев через TG_OWNER_IDS |
| **Группы** | Логирование + ответы по mention/reply |
| **Dual transport** | Telethon (userbot) + Bot API (aiogram) |
| **Skills** | Сложные сценарии через SKILL.md |
| **Браузер** | Персистентный Chromium с VNC |

---

## Транспорты

Два транспорта работают параллельно через единый Transport Protocol:

| Транспорт | Что нужно | Плюсы |
|-----------|-----------|-------|
| **Telethon** (userbot) | `TG_API_ID` + `TG_API_HASH` | Полный Telegram API, чтение чатов, поиск |
| **Bot** (aiogram 3.x) | `TG_BOT_TOKEN` | Bot API, mention в группах, MarkdownV2 |

Хотя бы один обязателен. Можно оба одновременно — каждый получает свою Claude-сессию.

`--setup` — принудительный re-login (Telethon QR/2FA).

---

## Мульти-пользовательская архитектура

Jobs работает с несколькими пользователями и владельцами. Каждый получает свою изолированную сессию.

### Owners (несколько владельцев)
```
TG_OWNER_IDS=[221820979,5119334106]

Полный доступ:
— Терминал, файлы, интернет
— Память, планировщик, MCP
— Управление задачами других
```

### External users (другие люди)
```
Ограниченный доступ:
— Jobs выясняет детали
— Отправляет тебе сводку
— Напоминает о задачах
```

### Групповые чаты
```
— Логирует ВСЕ сообщения в group_logs/{chat_id}.log
— Отвечает только на @mention / reply от owner_ids
— Одна Claude-сессия на чат
— Авторотация логов (1 MB → 500 KB)
```

### Сценарий (приватный)

```
Ты → Jobs: "Договорись с @masha о встрече завтра 12-20"
Jobs → @masha: "Привет! Нужно согласовать встречу. Удобные слоты: 12-20"
@masha → Jobs: "15:00 ок"

Jobs помнит весь контекст (persistent task session):
Jobs → Тебе: "Встреча с @masha в 15:00, напоминание создано"
Jobs → @masha: "Отлично, встреча в 15:00!"
```

### Сценарий (группа)

```
[Петя пишет в группу]
[Маша отвечает]
[ты пишешь @bot о чем говорили?]
Jobs → [читает лог группы, отвечает с контекстом]
```

---

## Конфигурация

| Переменная | Описание | Нужна |
|------------|----------|-------|
| `TG_API_ID` | [my.telegram.org](https://my.telegram.org) | Telethon |
| `TG_API_HASH` | [my.telegram.org](https://my.telegram.org) | Telethon |
| `TG_BOT_TOKEN` | [@BotFather](https://t.me/BotFather) | Bot |
| `TG_OWNER_IDS` | JSON array: `[123,456]` | Да |
| `TG_USER_ID` | Backward-compat (если TG_OWNER_IDS пуст) | Нет |
| `OPENAI_API_KEY` | Для голосовых (Whisper) | Нет |
| `HEARTBEAT_INTERVAL_MINUTES` | Проверки (0 = выкл) | Нет |

Хотя бы один транспорт обязателен (Telethon или Bot).

---

## Память

```
/workspace/
├── MEMORY.md          # Факты, предпочтения
├── memory/
│   └── 2024-01-15.md  # Дневной лог
└── sessions/
    └── *.md           # Транскрипты
```

Поиск: гибридный (vector + BM25).

---

## MCP интеграции

```
— Подключи postgres
— 🔍 Нашёл! Нужен connection string:
— postgresql://user:pass@host/db
— ✅ Готово, могу делать SQL
```

700+ серверов: [registry.modelcontextprotocol.io](https://registry.modelcontextprotocol.io)

---

## Браузер

Персистентный Chromium через @playwright/mcp.

```
— Открой hh.ru и найди вакансии python
— [snapshot → click → type → snapshot]
— Нашёл 47 вакансий, вот топ-10...

— Залогинься в twitter
— [navigate → snapshot → type login → click → ...]
— ✅ Залогинен, куки сохранены
```

### Архитектура

```
┌─────────────────────────────────────────┐
│           Docker: browser               │
│                                         │
│  Xvfb → Chromium → CDP (:9222)         │
│           ↓           ↓                 │
│        x11vnc    HAProxy (:9223)       │
│           ↓                             │
│      noVNC (:6080)                     │
└─────────────────────────────────────────┘
         ↓                    ↓
    Jobs (@playwright/mcp)   Ты (noVNC)
```

- **Playwright MCP** — snapshot + ref workflow (accessibility tree)
- **noVNC** — просмотр в браузере: http://localhost:6080
- **Профиль** — куки, сессии, история сохраняются

| Команда | Действие |
|---------|----------|
| `browser_navigate(url)` | Открыть страницу |
| `browser_snapshot()` | Accessibility-дерево с ref-ами |
| `browser_click(element, ref)` | Клик по ref |
| `browser_type(element, ref, text)` | Ввод текста |
| `browser_take_screenshot()` | Скриншот |
| `browser_evaluate(script)` | Выполнить JS |

---

## Плагины

```
— Найди плагины для code review
— 🔍 Найдено 2: code-review, pr-review-toolkit
— Установи code-review
— ✅ Установлен! Содержит: skills, agents
```

Плагины добавляют skills, commands, hooks, agents и MCP серверы.

| Команда | Действие |
|---------|----------|
| `plugin_search query` | Поиск в маркетплейсе |
| `plugin_install name` | Установить плагин |
| `plugin_list` | Показать установленные |
| `plugin_available` | Все доступные |

---

## Skills

Сложные сценарии через SKILL.md — SDK подхватывает автоматически.

```
— Договорись с @masha о встрече завтра 12-20
— [находит @masha, отправляет запрос]
— [ждёт ответа, согласовывает время]
— [создаёт напоминание за 5 мин]
— ✅ Встреча с @masha в 15:00
```

### Как работает

```
skills/schedule-meeting/SKILL.md
    │
    ▼ semantic match по description
    │
SDK активирует skill
    │
    ▼ Claude выполняет алгоритм
    │
Cross-session: отправляет запрос @masha
    │
    ▼ @masha отвечает
    │
Результат → owner
```

### Управление skills

```
— Создай skill для daily standup
— skill_create name="daily-standup" description="..." algorithm="..."
— ✅ Skill создан

— Покажи все skills
— skill_list
— • schedule-meeting, • daily-standup
```

| Команда | Действие |
|---------|----------|
| `skill_create` | Создать новый |
| `skill_list` | Показать все |
| `skill_show name` | Содержимое |
| `skill_edit name` | Редактировать |
| `skill_delete name` | Удалить |

Документация: `skills/CLAUDE.md`

---

## Структура

```
src/
├── main.py           # Точка входа
├── config.py         # Настройки (TG_OWNER_IDS, транспорты)
├── setup.py          # Интерактивный setup (--setup)
├── heartbeat.py      # Проактивные проверки
│
├── users/            # Мульти-сессии (private + group + task), задачи, промпты
│   └── sdk_compat.py # SDK monkey-patch (unknown message types)
├── telegram/         # Dual transport + handlers
│   ├── transport.py        # Transport Protocol + IncomingMessage
│   ├── telethon_transport.py  # Telethon (userbot)
│   ├── bot_transport.py       # aiogram (Bot API) + MarkdownV2
│   ├── handlers.py            # Routing: private / group / channel
│   ├── group_log.py           # Логирование групповых чатов + ротация
│   └── tools.py               # Telegram API tools для Claude
├── triggers/         # Unified trigger system (scheduler, heartbeat, tg_channel)
├── memory/           # Память + поиск
├── tools/            # Планировщик + разделение по ролям
├── mcp_manager/      # Внешние MCP интеграции
├── plugin_manager/   # Плагины из маркетплейса
└── skill_manager/    # Управление локальными skills

browser/              # Docker-контейнер с Chromium
├── Dockerfile
└── start-*.sh        # Скрипты запуска

skills/               # Skills для SDK
└── schedule-meeting/ # Согласование встреч
    └── SKILL.md
```

---

## Подписки на каналы

```
— Следи за @tech_news и делай сводку каждого поста
— ✅ Подписка создана

[новый пост в @tech_news]
— 💡 Сводка: Google выпустила Gemini 3.0...
```

| Команда | Действие |
|---------|----------|
| `subscribe_trigger(type, config, prompt)` | Подписаться |
| `unsubscribe_trigger(id)` | Отписаться |
| `list_triggers()` | Активные подписки |

---

## Команды агента

### Owner tools
| Команда | Действие |
|---------|----------|
| `create_task(user, title, kind, deadline)` | Создать задачу |
| `list_tasks(user, status)` | Задачи с фильтрами |
| `send_to_user(user, msg)` | Отправить сообщение |
| `resolve_user(query)` | Найти по имени/@username |
| `list_users()` | Все пользователи |
| `ban_user(user)` / `unban_user(user)` | Бан/разбан |
| `subscribe_trigger(type, config, prompt)` | Подписка на события |

### External user tools
| Команда | Действие |
|---------|----------|
| `get_my_tasks()` | Мои задачи |
| `update_task(task_id, status, result)` | Обновить задачу |
| `send_summary_to_owner(summary)` | Сводка владельцу |

---

## Roadmap

- [x] Claude Code в Telegram
- [x] Память + векторный поиск
- [x] Планировщик задач
- [x] MCP интеграции
- [x] Мульти-пользователи
- [x] Управление задачами
- [x] Skills (SDK native)
- [x] Cross-session communication
- [x] Браузер (@playwright/mcp + CDP)
- [x] Unified trigger system (подписки на каналы)
- [x] Persistent task sessions (контекст задач между сессиями)
- [x] Яндекс Телемост (через браузер — создание встреч)
- [x] Dual transport (Telethon + Bot API параллельно)
- [x] Групповые чаты (логирование + mention/reply)
- [x] Мульти-owner (TG_OWNER_IDS)
- [ ] Веб-интерфейс

---

## Лицензия

MIT

---

[@qanelph](https://github.com/qanelph)
